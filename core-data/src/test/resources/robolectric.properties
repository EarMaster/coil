# One below targetSdk, for the same reason as the app module's copy: Robolectric's SDK 36 image
# needs a newer JDK than the Temurin 17 every workflow here pins, so it would pass locally and
# throw on CI. SDK 35 runs on 17, which makes the two the same platform.
sdk=35
